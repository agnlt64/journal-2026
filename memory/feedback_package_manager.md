---
name: Package manager — npm
description: L'utilisateur utilise npm, pas pnpm
type: feedback
---

Utiliser `npm` (pas `pnpm`) pour toutes les commandes d'installation de dépendances.

**Why:** L'utilisateur a explicitement corrigé une commande `pnpm` en précisant qu'il utilise npm.

**How to apply:** Toujours écrire `npm install` au lieu de `pnpm add` ou `pnpm install`.
